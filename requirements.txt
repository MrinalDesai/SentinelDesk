# --- Core data contracts ---
pydantic>=2.6

# --- Deterministic NLP / ML spine ---
scikit-learn>=1.4
imbalanced-learn>=0.14       # SMOTE oversampling (agentic improve loop)
nltk>=3.8
rank-bm25>=0.2

# --- Retrieval & orchestration (live) ---
qdrant-client>=1.18          # vector store (runs embedded/in-memory; hosted cluster at scale)
sentence-transformers>=5.0   # BGE-M3 semantic embeddings
networkx>=3.2                # Graph RAG
langgraph>=0.2               # agent orchestration

# --- Service / API layer (live) ---
fastapi>=0.110
uvicorn>=0.29
cryptography>=42             # encryption-at-rest

# --- Genuinely optional / not yet wired (documented seams) ---
# presidio-analyzer>=2.2     # ML PII detection (roadmap; regex PII is implemented)
# presidio-anonymizer>=2.2
# spacy>=3.7
# psycopg2-binary            # PostgreSQL-backed RBAC (in-memory RBAC is implemented)
# ollama                     # local Mistral runtime (installed separately, not via pip)

# --- Dev ---
pytest>=8
