# --- Core data contracts (used now) ---
pydantic>=2.6

# --- Deterministic NLP / ML spine (next slices) ---
scikit-learn>=1.4
nltk>=3.8
rank-bm25>=0.2

# --- Service-dependent (validated on your machine, not in CI sandbox) ---
# ollama                 # local Mistral 7B Q8 runtime
# qdrant-client>=1.8     # vector store
# FlagEmbedding          # BGE-M3 embeddings
# presidio-analyzer>=2.2 # PII detection
# presidio-anonymizer>=2.2
# spacy>=3.7
# networkx>=3.2          # Graph RAG
# langgraph>=0.1         # agent orchestration
# psycopg2-binary        # PostgreSQL RBAC
# fastapi>=0.110
# uvicorn>=0.27
# streamlit>=1.32

# --- Dev ---
pytest>=8
langgraph>=0.2
cryptography>=42
